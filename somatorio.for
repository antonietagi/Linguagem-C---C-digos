#include <stdio.h>

int main(){
    int soma=0, n,i;
    printf("Digite um número:");
    scanf("%d", &n);
    
    for (i=1; i<=n; i++);
     soma += soma + (5+i*i+2*i+8);
     
     printf("O somatório é igual a: %d", soma);
    
}
 
