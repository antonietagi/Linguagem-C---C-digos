#include <stdio.h>

int main(){
    int a,b;
  printf("Digite dois valores:");
  scanf("%d %d", &a, &b);
  
  if(a>b){
      printf("O %d número é maior", a);
      
  }else if(b>a){
      printf("O %d número é maior", b);
          
  }else{
      printf("São iguais");
  }

    return 0;
}
